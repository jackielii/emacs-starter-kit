;; -*- mode: Lisp; -*-

(setq prj-version "0.4")

(setq prj-list
      '(("workflow-kit" "c:/pyworkspace/workflow-kit")))

(setq prj-last-open "workflow-kit")

(setq prj-frame-pos
      '((top + -4)
        (left + -4)
        (width . 168)
        (height . 44)))

